node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# ข้อมูลที่ dump ออกมาจาก Firestore มี password hash และอีเมลผู้ใช้จริง ห้ามขึ้น git
backup/

# ไฟล์ dump ของฐานข้อมูล มี password hash เลขบัตรที่เข้ารหัสไว้ และอีเมลผู้ใช้จริง
# repo นี้เป็น public การหลุดขึ้นไปครั้งเดียวคือต้องเปลี่ยนรหัสผ่านผู้ใช้ทุกคน
#
# เครื่องหมาย / นำหน้าจำกัดกฎไว้ที่รากเท่านั้น migrations/ กับ scripts/sql/
# จึงไม่ถูกกระทบ ซึ่งเป็นที่เดียวที่ไฟล์ .sql ควรอยู่
/*.sql
*.dump
*.dump.sql
duritrack-*.sql
